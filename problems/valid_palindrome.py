def is_palindrome(s: str) -> bool:
    """
    LeetCode 125 - Valid Palindrome (Easy)

    A phrase is a palindrome if, after converting all uppercase letters to
    lowercase and removing all non-alphanumeric characters, it reads the same
    forward and backward.

    Given a string `s`, return True if it is a palindrome, False otherwise.

    Example:
        s = "A man, a plan, a canal: Panama"
        -> True
        s = "race a car"
        -> False
        s = " "  (empty after filtering)
        -> True

    Constraints:
        - 1 <= len(s) <= 2 * 10^5
        - s consists of printable ASCII characters.

    Hint: str.isalnum() and str.lower() are useful. Try a two-pointer loop.
    """
    raise NotImplementedError
