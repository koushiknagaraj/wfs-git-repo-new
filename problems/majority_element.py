def majority_element(nums: list[int]) -> int:
    """
    LeetCode 169 - Majority Element (Easy)

    Given an array `nums` of size n, return the majority element — the value
    that appears more than n // 2 times. You may assume it always exists.

    Example:
        nums = [3, 2, 3] -> 3
        nums = [2, 2, 1, 1, 1, 2, 2] -> 2

    Constraints:
        - n == len(nums), 1 <= n <= 5 * 10^4
        - The majority element always exists in the array.

    Try it two ways for practice:
      1. A dict counting occurrences.
      2. (Bonus, harder) Boyer-Moore voting for O(1) space.
    """
    raise NotImplementedError
