def contains_duplicate(nums: list[int]) -> bool:
    """
    LeetCode 217 - Contains Duplicate (Easy)

    Given an integer array `nums`, return True if any value appears at least
    twice in the array, and False if every element is distinct.

    Example:
        nums = [1, 2, 3, 1] -> True
        nums = [1, 2, 3, 4] -> False

    Constraints:
        - 1 <= len(nums) <= 10^5

    Practice goal: use enumerate(nums) while writing this (even though a set
    alone solves it) — track index and value together, e.g. for a variant
    where you'd need positions too.
    """
    raise NotImplementedError
