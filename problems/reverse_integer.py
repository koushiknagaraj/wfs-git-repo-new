def reverse(x: int) -> int:
    """
    LeetCode 7 - Reverse Integer (Medium, but approachable)

    Given a signed 32-bit integer `x`, return `x` with its digits reversed.
    If reversing causes the value to go outside the signed 32-bit integer
    range [-2^31, 2^31 - 1], return 0.

    Example:
        x = 123   -> 321
        x = -123  -> -321
        x = 120   -> 21
        x = 1534236469 -> 0   (would overflow 32-bit range)

    Constraints:
        - -2^31 <= x <= 2^31 - 1
        - You may NOT use string reversal tricks like x[::-1] on the int
          directly cast to str... well, you can try it, but also try doing it
          with pure loop + arithmetic (% and //) for the practice.
    """
    raise NotImplementedError
